# PostgreSQL

1. Teste `pg_isready` no container.
2. Verifique conexoes em uso no dashboard.
3. Confirme se o usuario `secp_monitor` ainda possui `pg_monitor`.
4. Nao rode migrations, vacuum full ou reset sem janela e backup.

