# Disco do host

1. Execute `df -h` e `df -ih`.
2. Verifique crescimento de `/srv/observability/prometheus`.
3. Confirme retencao `15d/40GB`.
4. Nao apague volumes Docker sem backup e confirmacao explicita.

